---
phase: 03-ddpg-models
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_actor.py
  - models/actor.py
autonomous: true
requirements: [DDPG-01]
tags: [ddpg, actor, resnet18, coordconv, pytorch]

must_haves:
  truths:
    - "Actor accepts (batch, 7, 64, 64) and returns (batch, 40)"
    - "Actor output is in [0, 1] via sigmoid"
    - "Actor uses BatchNorm2d in its residual backbone"
    - "CoordConv prepends 2 coord channels so its inner Conv2d receives 9 channels"
  artifacts:
    - path: "models/actor.py"
      provides: "CoordConv, BasicBlock, Actor nn.Module classes"
      contains: "class Actor(nn.Module)"
      min_lines: 80
    - path: "tests/test_actor.py"
      provides: "DDPG-01 shape, range, and BatchNorm presence assertions"
      contains: "def test_actor_shape"
  key_links:
    - from: "models/actor.py::Actor.forward"
      to: "torch.sigmoid"
      via: "final head activation"
      pattern: "torch\\.sigmoid\\(self\\.fc"
    - from: "models/actor.py::CoordConv"
      to: "register_buffer"
      via: "device-agnostic coord grids"
      pattern: "register_buffer\\('(xx|yy)'"
---

<objective>
Implement the DDPG actor network (DDPG-01): a ResNet18-like CNN with a CoordConv stem and BatchNorm backbone that maps a 7-channel state observation to 40 stroke parameters in [0, 1] via sigmoid.

Purpose: The actor is the policy — it maps `s_t = (canvas, target, step_normalized)` to the `k=5 × STROKE_DIM=8 = 40` stroke params the agent proposes each step. It is the first of four DDPG components and provides the shared `CoordConv` and `BasicBlock` helpers consumed by the critic (Plan 03-03).
Output: `models/actor.py` (CoordConv, BasicBlock, Actor classes) + `tests/test_actor.py` (shape/range/BN assertions), all tests green.

## Phase Goal

**As a** training-loop builder, **I want to** instantiate an actor that maps a 7-channel state to 40 stroke params in [0,1], **so that** Phase 4 can sample actions from the policy and backpropagate through it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ddpg-models/03-CONTEXT.md
@.planning/phases/03-ddpg-models/03-RESEARCH.md
@.planning/phases/03-ddpg-models/03-PATTERNS.md
@models/renderer.py
@config.py
@tests/test_neural_renderer.py
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03-01)

New symbols created in `models/actor.py`:
- `class CoordConv(nn.Module)` — wraps a Conv2d, prepends normalized (x,y) coord channels via `register_buffer`. Constructor signature: `CoordConv(in_channels, out_channels, kernel_size=3, stride=1, padding=1, bias=False)`. Inner `nn.Conv2d` receives `in_channels + 2` input channels.
- `class BasicBlock(nn.Module)` — ResNet18 BasicBlock with BatchNorm2d (`expansion = 1`). Constructor: `BasicBlock(in_channels, out_channels, stride=1, downsample=None)`.
- `class Actor(nn.Module)` — full policy network. Constructor: `Actor()` (no args). `forward(x: Tensor) -> Tensor` maps `(B, 7, 64, 64) -> (B, 40)`.

New file: `tests/test_actor.py`.

`CoordConv` and `BasicBlock` are imported by `models/critic.py` in Plan 03-03 — they are the shared backbone helpers. Do not duplicate them in critic.py.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 scaffold — failing actor shape/range/BN tests</name>
  <files>tests/test_actor.py</files>
  <read_first>
    - tests/test_neural_renderer.py (exact test-file analog — import style, shape-assertion style, GPU skip pattern, no-fixture/no-main convention)
    - .planning/phases/03-ddpg-models/03-PATTERNS.md (test patterns section, lines 366-432)
    - config.py (IMG_SIZE, STROKE_DIM, STROKES_PER_STEP constants)
  </read_first>
  <behavior>
    - test_actor_shape: Actor().eval(); out = actor(torch.zeros(2, 7, 64, 64)); assert out.shape == (2, 40)
    - test_output_range: out of actor(torch.rand(8, 7, 64, 64)) has out.min() >= 0.0 and out.max() <= 1.0 (sigmoid bound)
    - test_actor_has_batchnorm: collect modules; assert at least one isinstance(m, torch.nn.BatchNorm2d) is present
    - test_coordconv_appends_two_channels: Actor's first CoordConv inner conv has in_channels == 9 (7 state + 2 coord)
    - test_actor_gpu: pytest.skip("No CUDA") guard; instantiate on cuda, forward (1,7,64,64), assert out.device.type == "cuda"
  </behavior>
  <action>
    Create tests/test_actor.py importing `torch`, `pytest`, `from models.actor import Actor, CoordConv` and `from config import IMG_SIZE, STROKE_DIM, STROKES_PER_STEP`. Write the five test functions listed in `<behavior>` using plain function-level instantiation (no fixtures, no `if __name__ == "__main__"` block), mirroring tests/test_neural_renderer.py exactly. Output count must equal `STROKES_PER_STEP * STROKE_DIM` (40) — reference the constant, do not hardcode 40 in the shape assertion comment. Call `.eval()` before forward in shape tests to simulate rollout (actor BatchNorm requires running stats at batch=1, but these tests use batch>=2 so eval is for convention). For test_coordconv_appends_two_channels, reach the inner Conv2d via the Actor's coord_conv attribute and assert `.conv.in_channels == 9`. These tests are RED by design until Task 2 implements models/actor.py.
  </action>
  <verify>
    <automated>python -m pytest tests/test_actor.py -x -q 2>&1 | grep -E "(ModuleNotFoundError|ImportError|collected)" || true</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_actor.py` defines functions: `test_actor_shape`, `test_output_range`, `test_actor_has_batchnorm`, `test_coordconv_appends_two_channels`, `test_actor_gpu`
    - File contains `from models.actor import Actor, CoordConv`
    - File contains `from config import` referencing `STROKES_PER_STEP` and `STROKE_DIM` (no hardcoded `40` literal in assertions — derive from constants)
    - `python -m pytest tests/test_actor.py -x -q` fails at collection or test time with ImportError/ModuleNotFoundError (RED — models/actor.py does not exist yet)
    - No `if __name__ == "__main__"` block present (grep returns 0 matches)
  </acceptance_criteria>
  <done>tests/test_actor.py exists with five test functions; pytest run fails only because models/actor.py is not yet implemented (RED state confirmed).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement models/actor.py (CoordConv + BasicBlock + Actor) — GREEN</name>
  <files>models/actor.py</files>
  <read_first>
    - models/renderer.py (nn.Module conventions, docstring style, register_buffer pattern lines 33-37, absolute config imports)
    - .planning/phases/03-ddpg-models/03-PATTERNS.md (actor.py architecture pattern lines 71-144 — full reference structure)
    - .planning/phases/03-ddpg-models/03-RESEARCH.md (Pattern 1 CoordConv lines 164-207, Pattern 2 BasicBlock lines 209-246, Pitfall 6 ResNet stage sizing lines 463-471)
    - .planning/phases/03-ddpg-models/03-CONTEXT.md (D-04, D-05, D-06 actor architecture decisions)
    - config.py (IMG_SIZE, STROKE_DIM, STROKES_PER_STEP)
    - tests/test_actor.py (the contract this implementation must satisfy)
  </read_first>
  <action>
    Implement models/actor.py with absolute imports `import torch`, `import torch.nn as nn`, `import torch.nn.functional as F`, `from config import IMG_SIZE, STROKE_DIM, STROKES_PER_STEP`.

    Per D-04, define `class CoordConv(nn.Module)` with constructor `CoordConv(in_channels, out_channels, kernel_size=3, stride=1, padding=1, bias=False)`. Build normalized coordinate grids with `torch.linspace(-1, 1, IMG_SIZE)`: xx shaped `(1,1,1,W)` expanded to `(1,1,H,W)`, yy shaped `(1,1,H,1)` expanded to `(1,1,H,W)`. Register both as buffers named `xx` and `yy` (`.contiguous()`) so they follow `.to(device)` — same register_buffer convention as models/renderer.py. Inner `self.conv = nn.Conv2d(in_channels + 2, out_channels, kernel_size, stride, padding, bias=bias)`. In `forward`, expand buffers to batch size B, `torch.cat([x, xx, yy], dim=1)` then apply conv.

    Per D-06, define `class BasicBlock(nn.Module)` with `expansion = 1`, constructor `BasicBlock(in_channels, out_channels, stride=1, downsample=None)`. Two `nn.Conv2d(..., 3, padding=1, bias=False)` each followed by `nn.BatchNorm2d`. forward: `F.relu(bn1(conv1(x)), inplace=True)`, then `bn2(conv2(...))`, apply downsample to identity if present, return `F.relu(out + identity, inplace=True)`.

    Per D-04/D-05, define `class Actor(nn.Module)`: first layer `CoordConv(7, 64, kernel_size=3, stride=2, padding=1)` reducing 64x64 -> 32x32 (CoordConv inner Conv2d gets 9 channels — 7 state + 2 coord). Then four ResNet18 stages via a `_make_layer(in_ch, out_ch, blocks, stride)` helper that builds a downsample `nn.Sequential(nn.Conv2d(in_ch, out_ch, 1, stride=stride, bias=False), nn.BatchNorm2d(out_ch))` when `stride != 1 or in_ch != out_ch`, then a list of `blocks` BasicBlocks (first carries the stride+downsample). Stages: layer1=(64->64, blocks=2, stride=1) -> 32x32; layer2=(64->128, blocks=2, stride=2) -> 16x16; layer3=(128->256, blocks=2, stride=2) -> 8x8; layer4=(256->512, blocks=2, stride=2) -> 4x4. Then `nn.AdaptiveAvgPool2d((1,1))` -> flatten -> `nn.Linear(512, STROKES_PER_STEP * STROKE_DIM)`. forward applies `F.relu(self.coord_conv(x), inplace=True)` then the four layers, pool, flatten(1), and returns `torch.sigmoid(self.fc(x))` -> (B, 40) in [0,1].

    Write a class docstring on Actor matching the renderer.py style: document Input `(B, 7, 64, 64)`, Output `(B, 40)` in `[0, 1]`. Do NOT add a NeuralRenderer-style alias. Do NOT use weight_norm here (that is the critic's concern in Plan 03-03).
  </action>
  <verify>
    <automated>python -m pytest tests/test_actor.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `models/actor.py` contains `class CoordConv(nn.Module)`, `class BasicBlock(nn.Module)`, `class Actor(nn.Module)`
    - `Actor().eval()(torch.zeros(2,7,64,64)).shape == (2, 40)` asserted by `test_actor_shape`
    - Actor output of `torch.rand(8,7,64,64)` has min >= 0.0 and max <= 1.0 (asserted by `test_output_range`)
    - At least one `torch.nn.BatchNorm2d` present in `Actor().modules()` (asserted by `test_actor_has_batchnorm`)
    - `Actor().coord_conv.conv.in_channels == 9` (asserted by `test_coordconv_appends_two_channels`)
    - `models/actor.py` contains `register_buffer('xx'` and `register_buffer('yy'`
    - `models/actor.py` contains `torch.sigmoid(self.fc`
    - `models/actor.py` does NOT contain `weight_norm` (grep -c after stripping comments returns 0)
    - `python -m pytest tests/test_actor.py -x -q` exits 0 (all GREEN)
  </acceptance_criteria>
  <done>models/actor.py implements CoordConv, BasicBlock, Actor; all five tests in tests/test_actor.py pass; output shape (B,40), range [0,1], BatchNorm present, CoordConv inner conv has 9 input channels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| training process → actor.forward | Tensor input from replay buffer / env; only internal float tensors cross, no external/user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Actor forward output (NaN propagation) | mitigate | sigmoid head bounds output to [0,1]; test_output_range asserts no out-of-range values, catching NaN-driven drift early |
| T-03-02 | Denial of Service | CoordConv buffer device mismatch | accept | register_buffer ensures coord grids follow .to(device); low risk — single local process, no concurrency |
| T-03-SC | Tampering | pip/conda installs | accept | No new packages introduced (RESEARCH.md Package Legitimacy Audit: torch + numpy already installed). No install task in this plan. |
</threat_model>

<verification>
- `python -m pytest tests/test_actor.py -x -q` exits 0
- `python -m pytest tests/ -x -q` exits 0 (no regression in existing suite)
- `models/actor.py` importable: `python -c "from models.actor import Actor, CoordConv, BasicBlock"`
</verification>

<success_criteria>
- Actor maps (batch, 7, 64, 64) → (batch, 40) in [0, 1] via sigmoid (ROADMAP Phase 3 success criterion 1, DDPG-01)
- CoordConv stem appends 2 coord channels (inner Conv2d gets 9 channels); BatchNorm present in backbone
- CoordConv and BasicBlock exported for reuse by Plan 03-03 critic
- All tests green; no regression
</success_criteria>

<output>
Create `.planning/phases/03-ddpg-models/03-01-SUMMARY.md` when done
</output>

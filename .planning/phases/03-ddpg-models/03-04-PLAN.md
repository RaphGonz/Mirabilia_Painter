---
phase: 03-ddpg-models
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-03"]
files_modified:
  - tests/test_agent.py
  - ddpg/agent.py
autonomous: true
requirements: [DDPG-03]
tags: [ddpg, agent, target-networks, soft-update, deepcopy, pytorch]

must_haves:
  truths:
    - "DDPGAgent creates actor, critic, and deepcopy'd actor_target + critic_target at init"
    - "Both target networks are permanently in eval() mode with requires_grad=False on all params"
    - "soft_update with τ=0.005 produces target params = (1-τ)·target_old + τ·source"
    - "update_step() exists as a scaffold that raises NotImplementedError (full actor/critic update is Phase 4 scope)"
  artifacts:
    - path: "ddpg/agent.py"
      provides: "DDPGAgent class + module-level soft_update function"
      contains: "class DDPGAgent"
      min_lines: 60
    - path: "tests/test_agent.py"
      provides: "DDPG-03 deepcopy, target eval mode, soft-update-correctness, NotImplementedError assertions"
      contains: "def test_soft_update"
  key_links:
    - from: "ddpg/agent.py::DDPGAgent.__init__"
      to: "copy.deepcopy"
      via: "target network construction"
      pattern: "copy\\.deepcopy"
    - from: "ddpg/agent.py::soft_update"
      to: "p_targ.data.mul_/add_"
      via: "in-place τ-weighted blend"
      pattern: "mul_\\(1"
---

<objective>
Implement the DDPG agent scaffold (DDPG-03): a `DDPGAgent` that owns the actor and critic, constructs deepcopy'd target networks frozen permanently in eval() mode, and applies the τ=0.005 soft update. The full actor/critic gradient `update_step()` (which needs the frozen SoftRasterizer) is Phase 4 scope — Phase 3 provides only the structural scaffold raising NotImplementedError.

Purpose: The agent is the training-loop scaffold. Phase 3 nails the target-network lifecycle (deepcopy-safe thanks to Plan 03-03's parametrizations.weight_norm) and the soft update so Phase 4 can drop in the Bellman update and policy gradient. This is the final Wave 3 plan — it wires together the actor (Plan 03-01) and critic (Plan 03-03).
Output: `ddpg/agent.py` (DDPGAgent class + soft_update function) + `tests/test_agent.py`, all tests green.

## Phase Goal

**As a** training-loop builder, **I want to** instantiate an agent with frozen deepcopy'd target networks and a working soft update, **so that** Phase 4 can fill in the Bellman/policy-gradient update against a stable target without re-solving the deepcopy or freeze lifecycle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ddpg-models/03-CONTEXT.md
@.planning/phases/03-ddpg-models/03-RESEARCH.md
@.planning/phases/03-ddpg-models/03-PATTERNS.md
@models/actor.py
@models/critic.py
@pretrain_renderer.py
@config.py
@tests/test_neural_renderer.py
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03-04)

New symbols created in `ddpg/agent.py`:
- `def soft_update(target: nn.Module, source: nn.Module, tau: float) -> None` — module-level function, `@torch.no_grad()`, in-place τ-weighted blend: `p_targ.data.mul_(1-tau).add_(tau * p.data)`.
- `class DDPGAgent` — constructor `DDPGAgent(device)`. Attributes: `actor`, `critic` (live nets on device), `actor_target`, `critic_target` (deepcopy'd, eval(), frozen), `actor_opt`, `critic_opt` (Adam optimizers with ACTOR_LR / CRITIC_LR). Methods: `update_step(self, batch)` — raises `NotImplementedError` with a message marking it Phase 4 scope (SoftRasterizer integration).

`ddpg/__init__.py` already exists (empty) — no need to create it.

Imports `Actor` from `models/actor.py` (Plan 03-01) and `Critic` from `models/critic.py` (Plan 03-03). The critic's `parametrizations.weight_norm` (Plan 03-03) is what makes `copy.deepcopy(self.critic)` safe here.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 scaffold — failing agent deepcopy/eval/soft-update tests</name>
  <files>tests/test_agent.py</files>
  <read_first>
    - tests/test_neural_renderer.py (test-file analog — import style, GPU skip, no fixtures, no main block)
    - .planning/phases/03-ddpg-models/03-PATTERNS.md (agent test cases lines 434-450 — test_soft_update and test_target_eval_mode reference impls)
    - .planning/phases/03-ddpg-models/03-RESEARCH.md (soft update verification lines 517-531, Pitfall 3 target grad lines 433-441)
    - .planning/phases/03-ddpg-models/03-CONTEXT.md (D-10, D-11, D-12, and Open Question 1 re: update_step scope lines 562-565)
    - config.py (TAU, ACTOR_LR, CRITIC_LR)
  </read_first>
  <behavior>
    - test_targets_are_deepcopies: agent = DDPGAgent(cpu); assert agent.actor_target is not agent.actor; assert agent.critic_target is not agent.critic; first param tensors are distinct objects but equal values at init
    - test_target_eval_mode: assert not agent.actor_target.training and not agent.critic_target.training
    - test_targets_frozen: all params of actor_target and critic_target have requires_grad == False
    - test_soft_update: clone first critic_target param; capture first critic param; call soft_update(agent.critic_target, agent.critic, TAU); assert allclose(p_after, (1-TAU)*p_before + TAU*p_source, atol=1e-6)
    - test_critic_deepcopy_safe: constructing DDPGAgent does not raise (proves Plan 03-03 WN parametrizations API works with copy.deepcopy)
    - test_update_step_not_implemented: pytest.raises(NotImplementedError): agent.update_step(None)
    - test_agent_gpu: pytest.skip("No CUDA") guard; DDPGAgent(cuda); assert next(agent.actor.parameters()).device.type == "cuda"
  </behavior>
  <action>
    Create tests/test_agent.py importing `torch`, `pytest`, `copy`, `from ddpg.agent import DDPGAgent, soft_update`, `from config import TAU`. Implement the seven test functions in `<behavior>`. Use `torch.device('cpu')` for non-GPU tests. For test_soft_update, follow the RESEARCH.md verification pattern exactly (lines 517-531): clone the target param before, read the source param, call soft_update, compare against `(1-TAU)*p_before + TAU*p_source`. For test_update_step_not_implemented use `with pytest.raises(NotImplementedError):`. No fixtures, no main block. RED until Task 2.
  </action>
  <verify>
    <automated>python -m pytest tests/test_agent.py -x -q 2>&1 | grep -E "(ModuleNotFoundError|ImportError|collected)" || true</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_agent.py` defines functions: `test_targets_are_deepcopies`, `test_target_eval_mode`, `test_targets_frozen`, `test_soft_update`, `test_critic_deepcopy_safe`, `test_update_step_not_implemented`, `test_agent_gpu`
    - File contains `from ddpg.agent import DDPGAgent, soft_update` and `from config import TAU`
    - test_soft_update asserts `torch.allclose(... (1 - TAU) * p_before + TAU * p_source, atol=1e-6)`
    - test_update_step_not_implemented uses `pytest.raises(NotImplementedError)`
    - `python -m pytest tests/test_agent.py -x -q` fails with ImportError/ModuleNotFoundError (RED — ddpg/agent.py not implemented)
    - No `if __name__ == "__main__"` block (grep returns 0 matches)
  </acceptance_criteria>
  <done>tests/test_agent.py exists with seven test functions covering deepcopy identity, eval mode, frozen params, soft-update correctness, deepcopy-safety, NotImplementedError scaffold, and GPU; pytest fails only because ddpg/agent.py is not yet implemented (RED confirmed).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ddpg/agent.py (DDPGAgent + soft_update) — GREEN</name>
  <files>ddpg/agent.py</files>
  <read_first>
    - models/actor.py (Actor constructor — Plan 03-01 artifact)
    - models/critic.py (Critic constructor + parametrizations.weight_norm that enables deepcopy — Plan 03-03 artifact)
    - pretrain_renderer.py (load_frozen_renderer freeze pattern lines 57-70 — eval() + requires_grad_(False) double-freeze to mirror)
    - .planning/phases/03-ddpg-models/03-PATTERNS.md (agent.py pattern lines 215-289 — target init, soft_update, update_step scaffold)
    - .planning/phases/03-ddpg-models/03-RESEARCH.md (Pattern 4 soft update lines 289-304, Pattern 6 deepcopy-safe init lines 363-382, Pitfall 1 deepcopy lines 409-421, Pitfall 3 target grad lines 433-441, Open Question 1 update_step scope lines 562-565)
    - .planning/phases/03-ddpg-models/03-CONTEXT.md (D-10, D-11, D-12; phase boundary stating env/training is Phase 4)
    - config.py (TAU, ACTOR_LR, CRITIC_LR, GRAD_CLIP_CRITIC, GAMMA)
    - tests/test_agent.py (contract to satisfy)
  </read_first>
  <action>
    Implement ddpg/agent.py with absolute imports `import copy`, `import torch`, `import torch.nn as nn`, `from models.actor import Actor`, `from models.critic import Critic`, `from config import TAU, ACTOR_LR, CRITIC_LR`.

    Per RESEARCH.md Pattern 4, define a module-level `@torch.no_grad()` function `def soft_update(target: nn.Module, source: nn.Module, tau: float) -> None` that loops `for p_targ, p in zip(target.parameters(), source.parameters())` and does `p_targ.data.mul_(1.0 - tau)` then `p_targ.data.add_(tau * p.data)` (in-place, no allocation — per RESEARCH.md Don't Hand-Roll).

    Per D-10/D-11, define `class DDPGAgent` with constructor `__init__(self, device)`. Store `self.device = device`. Create `self.actor = Actor().to(device)` and `self.critic = Critic().to(device)`. Construct targets via `self.actor_target = copy.deepcopy(self.actor)` and `self.critic_target = copy.deepcopy(self.critic)` — this is deepcopy-safe ONLY because Plan 03-03's critic uses `parametrizations.weight_norm` (RESEARCH.md Pitfall 1). Then permanently freeze both targets: call `.eval()` on each and loop `for p in target.parameters(): p.requires_grad_(False)` — mirror the double-freeze pattern from pretrain_renderer.py::load_frozen_renderer. Create optimizers `self.actor_opt = torch.optim.Adam(self.actor.parameters(), lr=ACTOR_LR)` and `self.critic_opt = torch.optim.Adam(self.critic.parameters(), lr=CRITIC_LR)`.

    Per CONTEXT.md phase boundary and RESEARCH.md Open Question 1, define `def update_step(self, batch)` as a SCAFFOLD that immediately `raise NotImplementedError(...)` with a message stating the full critic Bellman update + policy gradient through the frozen SoftRasterizer is Phase 4 scope. Include a structured comment block inside (or directly above) update_step documenting the intended Phase 4 sequence — critic MSE loss against `y = rew + GAMMA * critic_target(next_obs) * (~done)`, critic gradient clip at GRAD_CLIP_CRITIC, actor loss `-critic(render_next_state(actor(obs))).mean()`, then `soft_update(actor_target, actor, TAU)` and `soft_update(critic_target, critic, TAU)` — but DO NOT implement the body; it must raise. Do NOT import or call SoftRasterizer in Phase 3.

    Add a DDPGAgent class docstring noting: Phase 3 scope = target network scaffold + soft update only; update_step is a Phase 4 placeholder.
  </action>
  <verify>
    <automated>python -m pytest tests/test_agent.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `ddpg/agent.py` contains `def soft_update(` decorated with `@torch.no_grad()` and using `mul_(1` and `add_(`
    - `ddpg/agent.py` contains `class DDPGAgent` with `copy.deepcopy(self.actor)` and `copy.deepcopy(self.critic)`
    - `DDPGAgent(torch.device('cpu'))` constructs without raising (asserted by test_critic_deepcopy_safe)
    - `agent.actor_target.training is False` and `agent.critic_target.training is False` (asserted by test_target_eval_mode)
    - All `agent.actor_target` and `agent.critic_target` params have `requires_grad == False` (asserted by test_targets_frozen)
    - After `soft_update(agent.critic_target, agent.critic, TAU)`, first target param equals `(1-TAU)*before + TAU*source` within atol=1e-6 (asserted by test_soft_update)
    - `agent.update_step(None)` raises `NotImplementedError` (asserted by test_update_step_not_implemented)
    - `ddpg/agent.py` does NOT import or reference `SoftRasterizer`/`renderer` (grep returns 0 — Phase 4 scope boundary)
    - `python -m pytest tests/test_agent.py -x -q` exits 0 (all GREEN)
  </acceptance_criteria>
  <done>ddpg/agent.py implements soft_update and DDPGAgent with deepcopy'd, eval(), frozen target networks, Adam optimizers, and an update_step scaffold that raises NotImplementedError; all seven tests pass; no SoftRasterizer reference (Phase 4 boundary respected).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| critic init → agent deepcopy | Critic's WN parametrization crosses into copy.deepcopy — failure point if the deprecated WN API were used |
| source net → target net (soft_update) | Parameter values blended in-place; gradients must NOT flow into targets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | Gradient accumulation into target networks | mitigate | targets frozen via requires_grad_(False) + eval(); soft_update is @torch.no_grad(); test_targets_frozen asserts requires_grad False (RESEARCH.md Pitfall 3) |
| T-03-08 | Denial of Service | copy.deepcopy(critic) crash (deprecated WN) | mitigate | Plan 03-03 critic uses parametrizations.weight_norm; test_critic_deepcopy_safe asserts construction succeeds (RESEARCH.md Pitfall 1) |
| T-03-SC | Tampering | pip/conda installs | accept | No new packages (torch already installed; RESEARCH.md Package Legitimacy Audit). No install task in this plan. |
</threat_model>

<verification>
- `python -m pytest tests/test_agent.py -x -q` exits 0
- `python -m pytest tests/ -x -q` exits 0 (full phase suite green — depends on Plans 03-01 and 03-03 merged)
- `python -c "from ddpg.agent import DDPGAgent, soft_update; import torch; DDPGAgent(torch.device('cpu'))"` succeeds
</verification>

<success_criteria>
- Target networks are deepcopies of actor and critic, permanently in eval() mode and frozen (ROADMAP Phase 3 success criterion 3, DDPG-03)
- soft_update with τ=0.005 produces target params that are the weighted average of current and previous target params after a single call
- update_step is a NotImplementedError scaffold (full update is Phase 4 — boundary drawn explicitly)
- Agent construction is deepcopy-safe (proves Plan 03-03 WN API choice was correct)
- All tests green; full Phase 3 suite passes
</success_criteria>

<output>
Create `.planning/phases/03-ddpg-models/03-04-SUMMARY.md` when done
</output>

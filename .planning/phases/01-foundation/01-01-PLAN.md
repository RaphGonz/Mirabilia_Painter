---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - config.py
  - palette.py
  - models/__init__.py
  - ddpg/__init__.py
  - tests/__init__.py
  - tests/test_config.py
  - tests/test_palette.py
  - tests/test_renderer.py
  - tests/test_imports.py
autonomous: true
requirements: [FOUND-01, FOUND-02]
must_haves:
  truths:
    - "config.py is importable and exposes IMG_SIZE=64, STROKE_DIM=8, STROKES_PER_STEP=5, N_STROKES=40, IMAGE_RANGE=(0.0, 1.0)"
    - "palette.py exposes project_color(rgb, colorspace) returning the nearest palette color for rgb, oklab, and hsv without error"
    - "project_color on pure black in oklab returns a finite (non-NaN) tensor"
    - "All Wave 0 test files and the pytest config exist so renderer tests can run in Plan 02"
    - "config, palette, renderer importable in sequence with no circular import error"
  artifacts:
    - path: "config.py"
      provides: "Project-wide constants (single source of truth)"
      contains: "IMG_SIZE"
    - path: "palette.py"
      provides: "Manual RGB palette + colorspace-aware nearest-neighbor projection"
      exports: ["PALETTE", "project_color"]
    - path: "pyproject.toml"
      provides: "pytest configuration (testpaths)"
      contains: "testpaths"
    - path: "tests/test_renderer.py"
      provides: "FOUND-03 test battery (scaffold for Plan 02)"
      contains: "def test_draw_no_autograd"
  key_links:
    - from: "palette.py"
      to: "config.py"
      via: "from config import PALETTE_COLORSPACE"
      pattern: "from config import"
---

<objective>
Establish the project scaffold and the two logic-light foundation modules: `config.py` (constants only) and `palette.py` (colorspace-aware palette projection). Also create the complete Wave 0 test scaffold — pytest config, package init files, and all four test files — so the renderer plan (Plan 02) implements against tests that already exist.

This is the first half of the Walking Skeleton vertical slice: after this plan, a Python session can `import config`, `import palette`, call `project_color(...)` in all three colorspaces, and the full test scaffold is in place. Plan 02 then adds `renderer.py` to complete the slice (importable trio + a recognizable drawn rectangle).

Purpose: Every downstream module in all 5 phases imports `config.py`; `palette.py` is called by `eval.py` (Phase 5). These APIs must be stable from day one (per D-07, D-08).
Output: pyproject.toml, config.py, palette.py, models/__init__.py, ddpg/__init__.py, tests/__init__.py, tests/test_config.py, tests/test_palette.py, tests/test_renderer.py, tests/test_imports.py
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (01-01-01): Create project scaffold and config.py</name>
  <files>pyproject.toml, config.py, models/__init__.py, ddpg/__init__.py, tests/__init__.py, tests/test_config.py, tests/test_imports.py</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Minimal config.py code example; Recommended Project Structure; Validation Architecture)
    - .planning/phases/01-foundation/01-CONTEXT.md (locked decisions D-01, D-06, D-07, D-08)
    - .planning/phases/01-foundation/01-PATTERNS.md (config.py pattern; tests/test_config.py pattern; tests/test_imports.py pattern; pyproject.toml pattern)
  </read_first>
  <action>
    Create the flat-root scaffold per D-07. Create `config.py` with constants ONLY (zero imports, zero logic per the "no-logic constants module" shared pattern): `IMG_SIZE: int = 64` (FOUND-01), `STROKE_DIM: int = 8`, `STROKES_PER_STEP: int = 5`, `N_STROKES: int = 40` (per D-01: 40 steps x 5 strokes = 200 total), `IMAGE_RANGE: tuple = (0.0, 1.0)`, and `PALETTE_COLORSPACE: str = "rgb"` (default per D-06 and RESEARCH open-question resolution). Add `MIN_VISIBLE_STROKE_WIDTH` only as a comment, NOT as a constant (it is derivable, not configurable). Do NOT add derived constants like TOTAL_STROKES.
    Create empty `models/__init__.py` and `ddpg/__init__.py` (needed for future-phase package imports per D-08) and empty `tests/__init__.py`.
    Create `pyproject.toml` with a `[tool.pytest.ini_options]` table setting `testpaths = ["tests"]`.
    Create `tests/test_config.py` importing all six constants from config and asserting their exact values and types (use the PATTERNS.md test_config.py block verbatim — covers value asserts and `test_all_constants_types`).
    Create `tests/test_imports.py` with the four import tests from the PATTERNS.md test_imports.py block (config, palette, renderer importable + no-circular-import sequence). NOTE: this file references palette and renderer; palette is created in Task 2 of this plan and renderer in Plan 02, so test_imports.py will not fully pass until Plan 02 completes — this is expected per the dependency DAG.
    Use flat absolute imports only (`from config import ...`), never relative or package-prefixed (D-08).
  </action>
  <acceptance_criteria>
    - config.py contains `IMG_SIZE: int = 64`
    - config.py contains `STROKE_DIM: int = 8`, `STROKES_PER_STEP: int = 5`, `N_STROKES: int = 40`, `IMAGE_RANGE: tuple = (0.0, 1.0)`, `PALETTE_COLORSPACE: str = "rgb"`
    - config.py has zero import statements (verify: no line beginning with `import` or `from`)
    - pyproject.toml contains `testpaths = ["tests"]`
    - Files exist: models/__init__.py, ddpg/__init__.py, tests/__init__.py
    - `pytest tests/test_config.py -x` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_config.py -x -q</automated>
  </verify>
  <done>config.py exposes all six constants with correct values/types; scaffold (pyproject.toml, init files, test_config.py, test_imports.py) exists; test_config.py passes.</done>
</task>

<task type="auto">
  <name>Task 2 (01-01-02): Implement palette.py and its test battery</name>
  <files>palette.py, tests/test_palette.py</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 2: Colorspace-Aware Nearest-Neighbor Projection; Palette Module Skeleton; Pitfall 3 oklab NaN; Pitfall 4 cdist shape)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-05 physical palette as float tuples; D-06 three colorspaces)
    - .planning/phases/01-foundation/01-PATTERNS.md (palette.py pattern: imports, _PALETTE_SRGB storage, _srgb_to_linear, _linear_to_oklab, _rgb_to_hsv, project_color; tests/test_palette.py pattern)
    - palette.py (the file being created)
  </read_first>
  <action>
    Create `palette.py` with imports `import torch`, `import math`, `from config import PALETTE_COLORSPACE` (D-08 flat import). Define `_PALETTE_SRGB: list[tuple[float, float, float]]` as a clearly-marked placeholder of 5-6 float [0,1] colors (white, black, and a few primaries) with a comment instructing the user to replace with the actual ~40 physical paint mixer colors divided by 255 (per D-05 and RESEARCH open-question 1 — this is NOT a scope reduction; the placeholder is the documented Phase 1 approach and project_color is palette-size-agnostic). Build the tensor once at module load: `PALETTE: torch.Tensor = torch.tensor(_PALETTE_SRGB, dtype=torch.float32)` — do not convert per call.
    Implement helper functions verbatim from PATTERNS.md: `_srgb_to_linear` (inverse sRGB gamma via torch.where), `_linear_to_oklab` (LMS via M1, `.clamp(min=0.0).pow(1.0/3.0)` per Pitfall 3 NaN guard, then M2), and `_rgb_to_hsv` (branch-free torch.where).
    Implement `project_color(rgb, colorspace=PALETTE_COLORSPACE) -> torch.Tensor`: `q = torch.as_tensor(rgb, dtype=torch.float32).unsqueeze(0)` (Pitfall 4: cdist needs 2D), branch on colorspace ("oklab" -> linearize+oklab both query and palette; "hsv" -> rgb_to_hsv both; else "rgb" identity), `torch.cdist(q_conv, pal_conv).argmin(dim=-1).item()`, return `PALETTE[idx]` (shape (3,)). For an unsupported colorspace, raise ValueError so `test_project_color_invalid_colorspace` passes.
    Create `tests/test_palette.py` using the PATTERNS.md test_palette.py block verbatim (returns-tensor, in-palette, rgb/oklab/hsv shape, invalid-colorspace raises, black-oklab-no-NaN, accepts-tensor).
  </action>
  <acceptance_criteria>
    - palette.py contains `from config import PALETTE_COLORSPACE`
    - palette.py builds `PALETTE` as a module-level `torch.tensor(..., dtype=torch.float32)` (not per-call)
    - palette.py `_linear_to_oklab` contains `.clamp(min=0.0).pow(1.0 / 3.0)` (NaN guard)
    - palette.py `project_color` uses `torch.cdist` and `.unsqueeze(0)` on the query
    - project_color with an unrecognized colorspace raises ValueError
    - `pytest tests/test_palette.py -x` exits 0
    - `python -c "from palette import project_color; r=project_color((0.0,0.0,0.0), 'oklab'); assert r.shape==(3,) and not r.isnan().any()"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_palette.py -x -q</automated>
  </verify>
  <done>project_color returns the nearest palette color (shape (3,)) for rgb, oklab, and hsv; black-in-oklab is finite (no NaN); invalid colorspace raises ValueError; test_palette.py passes.</done>
</task>

<task type="auto">
  <name>Task 3 (01-01-04): Create the renderer test scaffold for Plan 02</name>
  <files>tests/test_renderer.py</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pytest test skeleton for renderer; Pitfall 1 subpixel degeneration)
    - .planning/phases/01-foundation/01-PATTERNS.md (tests/test_renderer.py pattern — copy entire block)
    - .planning/phases/01-foundation/01-VALIDATION.md (Per-Task Verification Map — Plan 02 tasks reference these test functions)
    - tests/test_renderer.py (the file being created)
  </read_first>
  <action>
    Create `tests/test_renderer.py` using the PATTERNS.md test_renderer.py block VERBATIM. It must import `from renderer import draw` and define exactly these functions so Plan 02 can verify against them: `test_draw_output_shape`, `test_draw_no_autograd`, `test_draw_paints_pixels`, `test_draw_full_canvas`, `test_draw_subpixel_stroke_is_empty`, `test_draw_values_in_range`, `test_draw_extreme_rotations`, `test_draw_gpu`.
    This is a Nyquist-rule scaffold: `renderer.py` does not exist yet (it is created in Plan 02), so this test file is expected to fail at collection (`ImportError`) until Plan 02 implements `draw`. That is intentional — the RED state for Plan 02. Do NOT create a stub renderer.py here; that belongs to Plan 02.
  </action>
  <acceptance_criteria>
    - tests/test_renderer.py contains `from renderer import draw`
    - tests/test_renderer.py defines `def test_draw_no_autograd`, `def test_draw_full_canvas`, `def test_draw_subpixel_stroke_is_empty`, `def test_draw_gpu`, `def test_draw_extreme_rotations`, `def test_draw_output_shape`, `def test_draw_paints_pixels`, `def test_draw_values_in_range`
    - `python -c "import ast; ast.parse(open('tests/test_renderer.py').read())"` exits 0 (file is syntactically valid Python)
    - `pytest tests/test_config.py tests/test_palette.py -x` exits 0 (the modules implemented in this plan still pass; renderer tests deliberately remain red until Plan 02)
  </acceptance_criteria>
  <verify>
    <automated>python -c "import ast,sys; ast.parse(open('tests/test_renderer.py').read()); src=open('tests/test_renderer.py').read(); sys.exit(0 if all(fn in src for fn in ['test_draw_no_autograd','test_draw_full_canvas','test_draw_subpixel_stroke_is_empty','test_draw_gpu','test_draw_extreme_rotations']) else 1)"</automated>
  </verify>
  <done>tests/test_renderer.py exists, is valid Python, imports draw from renderer, and defines all eight required test functions; the two already-implemented module test files still pass.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and files newly created by THIS plan (exclude from drift verification):

**Files:**
- `pyproject.toml`
- `config.py`
- `palette.py`
- `models/__init__.py`
- `ddpg/__init__.py`
- `tests/__init__.py`
- `tests/test_config.py`
- `tests/test_palette.py`
- `tests/test_renderer.py`
- `tests/test_imports.py`

**Constants (config.py):**
- `IMG_SIZE = 64`
- `STROKE_DIM = 8`
- `STROKES_PER_STEP = 5`
- `N_STROKES = 40`
- `IMAGE_RANGE = (0.0, 1.0)`
- `PALETTE_COLORSPACE = "rgb"`

**Symbols (palette.py):**
- `_PALETTE_SRGB` (placeholder list)
- `PALETTE` (torch.Tensor, shape (P, 3))
- `_srgb_to_linear(c)`
- `_linear_to_oklab(rgb_linear)`
- `_rgb_to_hsv(rgb)`
- `project_color(rgb, colorspace)`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Phase 1 has no network, no user input, no file uploads, no auth. All inputs are in-process float tensors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `palette._linear_to_oklab` (float ops near black) | mitigate | `.clamp(min=0.0)` before `.pow(1.0/3.0)` prevents NaN propagation; `test_project_color_black` asserts output is finite (per RESEARCH Pitfall 3) |
| T-01-02 | Tampering | `palette.project_color` input rgb | accept | PyTorch tensor ops are bounds-safe; out-of-range values still produce a valid nearest-neighbor; no raw memory access (per RESEARCH Security Domain) |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | Phase 1 installs zero new packages; all deps (torch, pytest, numpy) already present and slopcheck-[OK] per RESEARCH Package Legitimacy Audit |

No high-severity threats applicable for this plan.
</threat_model>

<verification>
- `pytest tests/test_config.py -x -q` exits 0
- `pytest tests/test_palette.py -x -q` exits 0
- `tests/test_renderer.py` exists and is syntactically valid (renderer tests stay red until Plan 02 — expected)
- `config.py` has zero imports
- `palette.py` imports only `torch`, `math`, and `from config import PALETTE_COLORSPACE`
</verification>

<success_criteria>
- FOUND-01: config.py exposes IMG_SIZE=64, STROKE_DIM=8, STROKES_PER_STEP=5, N_STROKES=40, IMAGE_RANGE=(0.0, 1.0) with correct types — `pytest tests/test_config.py -x` green
- FOUND-02: palette.py project_color returns nearest palette color for rgb/oklab/hsv without error, black-oklab is finite — `pytest tests/test_palette.py -x` green
- Wave 0 scaffold complete: pyproject.toml, all init files, all four test files exist
- Renderer test scaffold ready for Plan 02 (RED state)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
